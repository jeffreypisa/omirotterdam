{% extends "base.twig" %}

{% block content %}
<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
	<section class="mod-archive bg-greylight" data-scroll-section>
		<div class="container">
			{% if is_verhalenatlas_tax %}
				<h1 class="h2 mb-4 mt-3 text-center">
					{{ options.verhalenatlas_category_page_title ?: ((lang == 'nl_NL') ? 'Alle verhalen' : 'All stories') }}
				</h1>

				<div class="verhalenatlas-category-controls mb-5">
					{% if verhalenatlas_filters %}
						<div class="form-group mb-0">
							<form id="filter-select" class="category-dropdown" action="{{ currenturl }}" method="get">
								<select class="form-select" name="filter" id="filter" onchange="return this.form.submit()">
									<option value="" {% if selected_filter == '' %}selected{% endif %}>
										{% if lang == 'nl_NL' %}Alle filters{% else %}All filters{% endif %}
									</option>
									{% for item in verhalenatlas_filters %}
										<option value="{{ item.slug }}" {% if selected_filter == item.slug %}selected{% endif %}>
											{{ item.title }}
										</option>
									{% endfor %}
								</select>
							</form>
						</div>
					{% endif %}
					{% if verhalenatlas_archive_page_scroll_link %}
						<a href="{{ verhalenatlas_archive_page_scroll_link }}" class="btn btn-light">
							{{ options.verhalenatlas_archive_button_label ?: ((lang == 'nl_NL') ? 'Bekijk de gebieden' : 'View areas') }}
						</a>
					{% endif %}
				</div>

				<div class="row mt-2 anime-fadein">
					{% for item in posts %}
					<div class="col-md-6">
						{% include ['tease-verhalenatlas.twig'] %}
					</div>
					{% endfor %}
				</div>

				{% if verhalenatlas_routes_posts %}
					<div class="verhalenatlas-routes-strook mt-5 pt-2" id="verhalenatlas-routes">
						<h2 class="h2 mb-4">{{ verhalenatlas_routes_title }}</h2>
						<div class="row anime-fadein">
							{% for item in verhalenatlas_routes_posts %}
							<div class="col-md-6 col-lg-4">
								{% include ['tease-verhalenatlas.twig'] %}
							</div>
							{% endfor %}
						</div>
						{% if verhalenatlas_routes_overview_link %}
							<a href="{{ verhalenatlas_routes_overview_link }}" class="btn btn-light mt-3">
								{{ verhalenatlas_routes_overview_label }}
							</a>
						{% endif %}
					</div>
				{% endif %}
			{% else %}
				<h1 class="h2 mb-4 mt-3 text-center">
					{% if lang == 'nl_NL' %}Verhalenatlas{% else %}Story Atlas{% endif %}
				</h1>

				<div class="verhalenatlas-actions mb-4">
					{% if verhalenatlas_archive_category_link %}
						<a href="#verhalenatlas-gebieden" class="btn btn-light me-2 mb-2">
							{{ options.verhalenatlas_archive_button_label ?: ((lang == 'nl_NL') ? 'Bekijk de gebieden' : 'View areas') }}
						</a>
					{% endif %}
					{% if verhalenatlas_all_verhalen_link %}
						<a href="{{ verhalenatlas_all_verhalen_link }}" class="btn btn-light mb-2">
							{{ options.verhalenatlas_all_verhalen_button_label ?: ((lang == 'nl_NL') ? 'Alle verhalen' : 'All stories') }}
						</a>
					{% endif %}
				</div>

				{% if options.verhalenatlas_intro_content or options.verhalenatlas_intro_image %}
					<div class="row align-items-center verhalenatlas-intro mb-5">
						<div class="col-lg-7 mb-4 mb-lg-0">
							<div class="content">
								{{ options.verhalenatlas_intro_content }}
							</div>
						</div>
						<div class="col-lg-5">
							{% if options.verhalenatlas_intro_image %}
								<div class="verhalenatlas-intro-image ratio-16x9 bg-cover" style="background-image: url({{ Image(options.verhalenatlas_intro_image).src|resize(1280) }})"></div>
							{% endif %}
						</div>
					</div>
				{% endif %}

				<h2 id="verhalenatlas-gebieden" class="h2 mb-4 mt-2">{{ verhalenatlas_grid_title }}</h2>

				<div class="row mt-2 anime-fadein verhalenatlas-grid">
					{% for item in posts %}
					<div class="col-md-6 col-lg-4">
						{% include ['tease-verhalenatlas.twig'] %}
					</div>
					{% endfor %}
				</div>
			{% endif %}
			{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
		</div>
	</section>
	{% include 'pagebuilder.twig' %}
</article>
{% endblock %}
