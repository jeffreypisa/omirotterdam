{% if menu %}
	<ul class="nav">
		{% for item in items %}
			<li class="nav-item {{ item.classes | join(' ') }}">
				<a class="nav-link h2 mb-2" target="{{ item.target }}" href="{{ item.link }}">
					{{ item.title }}
				</a>
			</li>
		{% endfor %}
	</ul>
{% endif %}
