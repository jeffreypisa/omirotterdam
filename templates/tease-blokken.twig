<a href="{{ item.link.url }}" target="{{ item.link.target }}" class="tease tease-event noline">
	<div class="item">
		<div class="image-holder overlay-gradient ratio-4x3 mb-30">
			<div class="image" style="background-image: url({{ Image(item.afbeelding).src }})"></div>
			<div class="content">
				<h3 class="h5 mb-0">{{ item.titel }}</h3>
				<p class="mb-0"> {{ item.tekst }} </p>
				
				<div class="read-more weight-600">{{ item.link.title }} <i class="fas fa-arrow-right"></i></div>
			</div>
		</div>
	</div>
</a>