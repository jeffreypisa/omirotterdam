{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
			<section class="mod-content bg-greylight" data-scroll-section>
				<div class="container text-center">
					<div class="row">
						<div class="col-md-10 offset-md-1">
							<h1 class="h2 mb-1 mt-3"> {{ post.title }} </h1>
							{% if post.van_tot %}
								<h3 class="weight-400 mb-3"> {{ post.van_tot }} </h3>
							{% endif %}
						</div>
					</div>
				</div>
			</section>
			
			{% for item in post.get_field('contentblokken') %}
				{% if item.acf_fc_layout == 'tekst' %}
					{% include 'partial/tekst.twig' %}	
					
				{% elseif item.acf_fc_layout == 'afbeelding' %}
					{% include 'partial/afbeelding.twig' %}	
				
				{% elseif item.acf_fc_layout == 'video' %}
					{% include 'partial/video.twig' %}
					
				{% elseif item.acf_fc_layout == 'knop' %}
					{% include 'partial/knop.twig' %}
					
				{% endif %}
			{% endfor %}
			
			{% if post.colofon %}
				<section class="mod-content bg-greylight text-sm" data-scroll-section>		
					<div class="container mt-5">
						<div class="row">
							<div class="col-md-10 offset-md-1 col-xl-8 offset-xl-2">
								<h6> Colofon </h6>
								<div class="row">
									<div class="col-auto col-sm-5 col-md-4 col-lg-3 weight-600">
										{% for item in post.get_field('colofon') %}
											{{ item.label }}: <br>
										{% endfor %}
									</div>
									<div class="col">
										{% for item in post.get_field('colofon') %}
											{{ item.tekst }} <br>
										{% endfor %}
									</div>
								</div>
							</div>
						</div>
					</div>
				</section>
			{% endif %}
			
			{% if options.logos %}
				<section class="mod-content bg-greylight text-sm" data-scroll-section>		
					<div class="row g-1">
						{% for logo in options.logos %}
							<div class="col">
								<a href="{{ logo.url }}" target="_blank" class="logo" style="background-image: url({{ Image(logo.logo).src }})">
							</a>
							</div>
						{% endfor %}
					</div>
				</section>
			{% endif %}
			
			{% include 'pagebuilder.twig' %}
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}
